---
import Layout from '@alexgodfrey/web/layouts/BaseLayout.astro';
import { socialImageVersion } from '@alexgodfrey/web/lib/og/note-social-image';
import { getPublishedNotes, publishedNoteSlug } from '@alexgodfrey/web/lib/published-notes';
import { absoluteUrl, articleStructuredData, siteContent } from '@alexgodfrey/web/lib/site-content';
import { validateNote } from '@alexgodfrey/web/lib/validators';
import { render } from 'astro:content';

export const prerender = true;

export async function getStaticPaths() {
  const allNotes = await getPublishedNotes();
  return allNotes.map((note) => {
    const slug = publishedNoteSlug(note);
    return {
      params: { slug },
      props: { note },
    };
  });
}

const { note } = Astro.props;
const frontmatter = validateNote(note.data);
const { Content } = await render(note);
const slug = publishedNoteSlug(note);
const markdownUrl = `/blog/${slug}.md`;
const socialImagePath = `/og/${slug}.png?v=${socialImageVersion({
  slug,
  title: frontmatter.title,
  description: frontmatter.description,
  publishedDate: frontmatter.pubDate.toISOString(),
  updatedDate: frontmatter.updatedDate?.toISOString(),
})}`;
const socialImageAlt = `${frontmatter.title} — an article by Alex Godfrey`;
const structuredData = articleStructuredData({
  title: frontmatter.title,
  description: frontmatter.description,
  slug,
  publishedDate: frontmatter.pubDate,
  updatedDate: frontmatter.updatedDate,
  imageUrl: absoluteUrl(socialImagePath),
});
---

<Layout
  title={`${frontmatter.title} | ${siteContent.name}`}
  description={frontmatter.description}
  ogImage={socialImagePath}
  ogImageAlt={socialImageAlt}
  ogImageWidth={1200}
  ogImageHeight={630}
  ogType="article"
  publishedTime={frontmatter.pubDate}
  modifiedTime={frontmatter.updatedDate}
  markdownUrl={markdownUrl}
  structuredData={structuredData}
>
  <article class="max-w-3xl mx-auto px-4 py-8">
    <header class="mb-8">
      <div class="mb-6 flex items-center justify-end gap-2">
        <a
          href={markdownUrl}
          type="text/markdown"
          class="px-3 py-1.5 text-xs font-mono text-muted-foreground hover:text-foreground border border-border rounded-md hover:bg-muted/50 transition-colors"
        >
          Markdown
        </a>
        <button
          id="copy-markdown-btn"
          data-markdown-url={markdownUrl}
          class="px-3 py-1.5 text-xs font-mono text-muted-foreground hover:text-foreground border border-border rounded-md hover:bg-muted/50 transition-colors"
        >
          Copy Markdown
        </button>
      </div>
      <h1 class="font-mono font-semibold text-3xl text-foreground mb-2">
        {frontmatter.title}
      </h1>
      {
        frontmatter.description && (
          <p class="font-mono text-sm text-muted-foreground mb-4">{frontmatter.description}</p>
        )
      }
      <div
        class="flex flex-wrap items-center gap-x-3 gap-y-1 font-mono text-xs text-muted-foreground"
      >
        <a href="/" rel="author" class="underline underline-offset-4 hover:text-foreground">
          By {siteContent.name}
        </a>
        <time datetime={frontmatter.pubDate.toISOString()}>
          Published: {frontmatter.pubDate.toISOString().split('T')[0]}
        </time>
        {
          frontmatter.updatedDate && (
            <time datetime={frontmatter.updatedDate.toISOString()}>
              Updated: {frontmatter.updatedDate.toISOString().split('T')[0]}
            </time>
          )
        }
      </div>
    </header>
    <div
      class="max-w-none [&_h2]:mt-8 [&_h2]:mb-3 [&_h2]:text-xl [&_h2]:font-semibold [&_p]:mb-4 [&_a]:underline [&_a]:underline-offset-4 [&_a:hover]:text-muted-foreground"
    >
      <Content />
    </div>
  </article>
</Layout>

<script>
  const attachCopyHandler = () => {
    const copyBtn = document.getElementById('copy-markdown-btn');

    if (!copyBtn) {
      return;
    }

    if (copyBtn.dataset.copyHandlerAttached === 'true') {
      return;
    }

    const handleClick = async (event: MouseEvent) => {
      event.preventDefault();
      try {
        const markdownUrl = copyBtn.dataset.markdownUrl;
        if (!markdownUrl) {
          throw new Error('Markdown URL is unavailable.');
        }
        const response = await fetch(markdownUrl, {
          headers: { Accept: 'text/markdown' },
        });
        if (!response.ok) {
          throw new Error(`Markdown request failed with ${response.status}.`);
        }
        const markdown = await response.text();
        await navigator.clipboard.writeText(markdown);
        const originalText = copyBtn.textContent;
        copyBtn.textContent = 'Copied!';
        copyBtn.classList.add('text-green-500');
        setTimeout(() => {
          copyBtn.textContent = originalText;
          copyBtn.classList.remove('text-green-500');
        }, 2000);
      } catch (err) {
        console.error('Failed to copy:', err);
        copyBtn.textContent = 'Failed';
        setTimeout(() => {
          copyBtn.textContent = 'Copy Markdown';
        }, 2000);
      }
    };

    copyBtn.addEventListener('click', handleClick);
    copyBtn.dataset.copyHandlerAttached = 'true';
  };

  attachCopyHandler();
</script>
