{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local", "pnpm-lock.yaml", ".env*"],
  "globalEnv": [
    "CHATDB_URL",
    "AGENTDB_API_URL",
    "AGENTDB_API_KEY",
    "ASTRO_CSP_MODE",
    "ASTRO_KEY",
    "ASTRO_LOG_FORMAT",
    "DATABASE_URL",
    "NEWSLETTER_ALLOWED_ORIGINS",
    "NEWSLETTER_RATE_LIMIT_SECRET",
    "NEWSLETTER_TURNSTILE_MODE",
    "NEWSLETTER_TURNSTILE_SECRET_KEY",
    "PUBLIC_NEWSLETTER_TURNSTILE_SITE_KEY",
    "RESEND_API_KEY"
  ],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^dev"]
    },
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".vercel/**", ".output/**"]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    },
    "format": {
      "dependsOn": ["^format"],
      "outputs": []
    },
    "format:check": {
      "dependsOn": ["^format:check"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^test"],
      "outputs": ["coverage/**"]
    },
    "clean": {
      "dependsOn": ["^clean"],
      "outputs": []
    }
  }
}
